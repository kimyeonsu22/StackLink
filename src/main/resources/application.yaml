spring:
  application:
    name: StackLink
  server:
    port: 8080
  datasource:
    url: jdbc:mysql://localhost:3306/stacklink?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: chaerin0605
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 10MB
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: TRACE
    org.springframework.web: DEBUG

jwt:
  secret: stacklink-secret-key-stacklink-secret-key-2026
  access-token-validity: 3600000
  refresh-token-validity: 1209600000